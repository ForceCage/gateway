# docs

Repository assets referenced by the top-level `README.md`.

- **`banner.png`** — hero banner shown at the top of the README
  ("The cost-governance proxy for AI agents"). Drop the PNG here so the
  README image renders on GitHub.
